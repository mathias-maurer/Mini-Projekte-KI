Einfache Beispiele für die Objekterkennung mit YOLO

Tutorial für die Klassifizierung von Objekten in gespeicherten Bildern, Kamerabildern und Videostreams.

Für die Klassifizierung wird ein vortrainiertes CNN verwendet.

Das Tutorial liegt als Jupyter Notebook und in Form einzelner Python Files vor (Unterordner Python). 